# MVP (must-ship)

This is the canonical list of what **must** be done to ship the MVP.

## How this works (low-breakage)

- MVP work is tracked primarily via **story metadata tags**, not file paths.
- A story is MVP if its metadata includes `release: mvp`.
- This file is a human-friendly index that references stories by **stable story ID** (e.g. `ML-001`), so renaming/moving story files doesn’t break tracking.

## MVP story index (by ID)

Add stories here as you qualify them as MVP.

- `AUTH-___` —
- `ONB-___` —
- `ML-___` —
- `PLAN-___` —
- `NUTR-___` —

## MVP scope reminders (existing docs)

- Feature inventory and “Core MVP Features” note: `docs/FEATURES.md`
- Roadmap priorities: `docs/roadmap/roadmap.md`
- MVP PRDs (initiative-level):
  - `docs/prds/conversational-onboarding.md`
  - `docs/prds/meal-planning-chat.md`

