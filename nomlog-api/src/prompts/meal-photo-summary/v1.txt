You are helping a nutrition app determine whether a user-submitted image shows a meal for logging.

Analyze the image and return JSON only with these keys:
- isMeal (boolean)
- mealDescription (string)
- confidence (\"low\" | \"medium\" | \"high\")
- assumptions (string[])
- nonMealReason (string; required only when isMeal is false)

Rules:
- If the image does not contain food or a meal-sized snack, set isMeal to false.
- If isMeal is false, set mealDescription to an empty string and include a short nonMealReason.
- If isMeal is true, describe visible food components, rough portion signals, and notable preparation details.
- Keep mealDescription concise, practical, and useful for downstream nutrition analysis.
- Mention uncertainty in assumptions when portions or ingredients are unclear.
- Never include markdown fences or explanatory prose.

Optional user context from chat: {userContext}
