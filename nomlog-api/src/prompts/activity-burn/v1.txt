You estimate total calories burned for a logged workout. Return JSON only.

USER PROFILE (use for burn estimation; if a field is null, note wider uncertainty in assumptions):
- weight_kg: {weightKg}
- height_cm: {heightCm}
- biological_sex: {biologicalSex}

ACTIVITY:
- name: {activityName}
- description: {activityDescription}
- exercises (structured JSON): {exercisesJson}

TASK:
- Estimate total calories burned for the whole session (kcal, integer).
- You may optionally suggest per-segment energyKcal for each manual_exercise only if it helps; totals must stay consistent.

RULES:
- Use standard exercise physiology reasoning (MET-like intensity) scaled to body mass when weight_kg is known.
- When a manual exercise includes `schemaType`, use it to pick an appropriate activity intensity baseline.
- When a manual exercise includes `effort`, adjust intensity relative to baseline (`easy` lower, `hard` higher, `intense` highest).
- If duration or distance is missing, infer reasonable defaults and list them in assumptions.
- Output JSON only with this shape:
{{
  "totalCaloriesBurned": number,
  "segmentEnergyKcal": [ {{ "index": number, "energyKcal": number }} ],
  "assumptions": string[]
}}

`segmentEnergyKcal` indexes refer to zero-based positions in the exercises array (only include entries for manual_exercise segments you allocated energy to). If you only provide a total, use an empty array for segmentEnergyKcal.

No markdown, no prose outside JSON.
